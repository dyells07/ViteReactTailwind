import React from 'react'

const UserContext = React.createContext()

export default UserContext;


//Context is Global Variable if we create COntext we should have provide Provider
{/* <UserContext>

</UserContext> */}